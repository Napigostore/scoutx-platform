apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: scoutx-staging

patches:
  - path: deployment-patch.yaml
  - path: configmap-patch.yaml

images:
  - name: scoutx-platform
    newName: scoutx-platform
    newTag: staging-latest
