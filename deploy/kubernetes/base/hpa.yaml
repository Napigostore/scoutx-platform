apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: scoutx-web
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: scoutx-web
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
